info:
  name: Change_password_weak
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/users/{{current_user_id}}/change-password"
  body:
    type: multipart-form
    data:
      - name: currentPassword
        type: text
        value: "{{password}}"
      - name: newPassword
        type: text
        value: "12345678"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Weak new password rejected", function () {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
