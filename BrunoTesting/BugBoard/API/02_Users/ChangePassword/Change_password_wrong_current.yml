info:
  name: Change_password_wrong_current
  type: http
  seq: 1

http:
  method: PUT
  url: "{{baseURL}}/users/me/password"
  body:
    type: json
    data: '{"currentPassword":"wrong-pass","newPassword":"NewStrongPass123!"}'
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Wrong current password rejected", function () {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
