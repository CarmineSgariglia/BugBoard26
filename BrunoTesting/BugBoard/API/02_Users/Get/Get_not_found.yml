info:
  name: Get_not_found
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/users/{{invalid_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Get invalid user not found/forbidden", function () {
          expect([403, 404]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
