info:
  name: Update_other_forbidden_for_dev
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseURL}}/users/{{target_user_id}}"
  body:
    type: multipart-form
    data:
      - name: firstName
        type: text
        value: UnauthorizedEdit
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Update other user role aware", function () {
          const email = bru.getEnvVar("email") || "";
          if (email.includes("admin")) {
            expect(res.getStatus()).to.equal(200);
          } else {
            expect([403, 404]).to.include(res.getStatus());
          }
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
