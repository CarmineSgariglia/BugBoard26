info:
  name: Update_self
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{baseURL}}/users/{{current_user_id}}"
  body:
    type: multipart-form
    data:
      - name: firstName
        type: text
        value: Bruno
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Self update returns 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
