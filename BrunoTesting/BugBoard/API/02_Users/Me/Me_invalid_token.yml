info:
  name: Me_invalid_token
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/users/me"
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: req.setHeader("Authorization", "Bearer {{invalid_token}}");
    - type: tests
      code: |-
        test("Me with invalid token rejected", function () {
          expect([401, 403]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
