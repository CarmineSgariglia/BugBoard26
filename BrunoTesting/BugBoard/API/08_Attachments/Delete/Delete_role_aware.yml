info:
  name: Delete_role_aware
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseURL}}/attachments/{{attachment_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Attachment delete status", function () {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
