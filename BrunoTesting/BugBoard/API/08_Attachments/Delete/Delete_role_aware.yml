info:
  name: Delete_role_aware
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseURL}}/issues/{{issue_id}}/attachments/{{attachment_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Attachment delete status", function () {
          expect([204, 403, 404]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
