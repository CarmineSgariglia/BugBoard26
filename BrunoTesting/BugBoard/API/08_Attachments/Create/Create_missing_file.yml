info:
  name: Create_missing_file
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/attachments"
  body:
    type: multipart-form
    data:
      - name: issueId
        type: text
        value: "{{issue_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Attachment create without file rejected", function () {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
