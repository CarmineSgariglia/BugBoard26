info:
  name: List_auth
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/attachments?issueId={{issue_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Attachments list status", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("Attachments list returns array", function () {
          expect(res.getBody()).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
