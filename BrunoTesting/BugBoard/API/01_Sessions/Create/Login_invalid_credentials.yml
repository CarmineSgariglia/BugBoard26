info:
  name: Create_invalid_credentials
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/sessions"
  body:
    type: json
    data: '{"email":"{{email}}","password":"wrong-password"}'
  auth:
    type: bearer
    token: ""

runtime:
  scripts:
    - type: tests
      code: |-
        test("Invalid credentials rejected", function () {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
