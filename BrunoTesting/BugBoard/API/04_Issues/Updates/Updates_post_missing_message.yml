info:
  name: Updates_post_missing_message
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/issues/{{issue_id}}/updates"
  body:
    type: multipart-form
    data:
      - name: message
        type: text
        value: ""
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Issue update comment requires message", function () {
          const email = bru.getEnvVar("email") || "";
          if (email.includes("admin")) {
            expect(res.getStatus()).to.equal(400);
          } else {
            expect(res.getStatus()).to.equal(403);
          }
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
