info:
  name: Updates_get
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/issues/{{issue_id}}/updates"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Issue updates get status", function () {
          expect([200, 403, 404]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
