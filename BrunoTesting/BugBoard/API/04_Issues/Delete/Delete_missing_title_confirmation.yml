info:
  name: Delete_missing_title_confirmation
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseURL}}/issues/{{issue_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Issue delete without title confirmation rejected", function () {
          expect([400, 403, 404]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
