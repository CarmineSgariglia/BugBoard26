info:
  name: Assign_empty_user_ids
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/issues/{{issue_id}}/assign"
  body:
    type: multipart-form
    data:
      - name: userIds
        type: text
        value: "[]"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Assign empty users rejected", function () {
          expect([400, 403, 404]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
