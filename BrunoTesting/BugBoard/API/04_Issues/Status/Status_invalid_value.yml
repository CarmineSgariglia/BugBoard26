info:
  name: Status_invalid_value
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{baseURL}}/issues/{{issue_id}}"
  body:
    type: json
    data: '{"status":"INVALID","message":"Invalid status attempt"}'
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Invalid status rejected", function () {
          expect([400, 403]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
