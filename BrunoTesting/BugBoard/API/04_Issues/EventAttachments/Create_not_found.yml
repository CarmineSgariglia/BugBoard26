info:
  name: Create_not_found
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/issues/{{issue_id}}/events/{{invalid_id}}/attachments"
  body:
    type: multipart-form
    data: []
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Event attachment upload returns 404 when event is missing", function () {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
