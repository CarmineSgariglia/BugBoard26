info:
  name: List_auth
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/notifications"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Notifications list returns 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("Notifications list is array", function () {
          expect(res.getBody()).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
