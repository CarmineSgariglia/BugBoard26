info:
  name: Delete_not_found
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseURL}}/notifications/{{invalid_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Delete missing notification returns 404", function () {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
