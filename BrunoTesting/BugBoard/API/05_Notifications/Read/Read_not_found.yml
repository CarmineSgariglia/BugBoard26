info:
  name: Read_not_found
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/notifications/{{invalid_id}}/read"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Read missing notification returns 404", function () {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
