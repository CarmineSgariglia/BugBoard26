info:
  name: Update_not_found
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseURL}}/notifications/{{invalid_id}}"
  body:
    type: json
    data: '{"isRead":true}'
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Update missing notification returns 404", function () {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
