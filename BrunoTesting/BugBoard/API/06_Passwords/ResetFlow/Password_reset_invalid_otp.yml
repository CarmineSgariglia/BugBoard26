info:
  name: Reset_invalid_otp
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/password-resets"
  body:
    type: json
    data: '{"email":"{{email}}","code":"000000","newPassword":"NewStrongPass123!"}'
  auth:
    type: bearer
    token: ""

runtime:
  scripts:
    - type: tests
      code: |-
        test("Password reset invalid otp rejected", function () {
          expect([400, 429]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
