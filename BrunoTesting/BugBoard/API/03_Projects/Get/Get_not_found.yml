info:
  name: Get_not_found
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/projects/{{invalid_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Get invalid project returns 404", function () {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
