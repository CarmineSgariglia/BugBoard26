info:
  name: Get_forbidden_user
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/projects/{{project_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Get project in restricted context is handled", function () {
          const status = res.getStatus();
          expect([200, 403, 404]).to.include(status);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
