info:
  name: Get_forbidden_user
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/projects/{{project_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Get project reflects current visibility scope", function () {
          expect([200, 403]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
