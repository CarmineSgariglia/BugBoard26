info:
  name: Get_forbidden_user
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/projects/{{project_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Get project in restricted context is forbidden for outsider", function () {
          expect(res.getStatus()).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
