info:
  name: Delete_not_found
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseURL}}/projects/{{invalid_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Delete invalid project returns not found or forbidden", function () {
          expect([403, 404]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
