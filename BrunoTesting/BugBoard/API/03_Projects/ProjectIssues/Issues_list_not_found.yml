info:
  name: Issues_list_not_found
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/projects/{{invalid_id}}/issues"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Issues list with invalid project returns 404", function () {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
