info:
  name: Issues_list_noAuth
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/projects/{{project_id}}/issues"
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        req.setHeader("Authorization", "");
    - type: tests
      code: |-
        test("Issues list without auth is rejected", function () {
          expect([401, 403]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
