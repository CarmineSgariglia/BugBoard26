info:
  name: Logout_legacy_redirect
  type: http
  seq: 99

http:
  method: POST
  url: "{{baseURL}}/auth/logout"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Legacy logout endpoint still valid", function () {
          expect([204, 401, 403]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
