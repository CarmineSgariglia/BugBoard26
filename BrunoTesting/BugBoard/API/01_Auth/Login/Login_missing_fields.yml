info:
  name: Login_missing_fields
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/auth/login"
  body:
    type: multipart-form
    data:
      - name: email
        type: text
        value: ""
      - name: password
        type: text
        value: ""
  auth:
    type: bearer
    token: ""

runtime:
  scripts:
    - type: tests
      code: |-
        test("Missing login fields rejected", function () {
          expect([400, 401]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
