info:
  name: Login_legacy_missing_field
  type: http
  seq: 100

http:
  method: POST
  url: "{{baseURL}}/auth/login"
  body:
    type: multipart-form
    data:
      - name: email
        type: text
        value: ""
      - name: password
        type: text
        value: ""
  auth:
    type: bearer
    token: ""

runtime:
  scripts:
    - type: tests
      code: |-
        test("Legacy missing fields rejected", function () {
          expect([400, 401]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
