info:
  name: Me_legacy_invalid_token
  type: http
  seq: 99

http:
  method: GET
  url: "{{baseURL}}/auth/me"
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: req.setHeader("Authorization", "Bearer {{invalid_token}}");
    - type: tests
      code: |-
        test("Legacy me invalid token rejected", function () {
          expect([401, 403]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
