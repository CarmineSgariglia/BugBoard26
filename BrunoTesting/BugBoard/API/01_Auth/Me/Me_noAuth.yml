info:
  name: Me_legacy_no_auth
  type: http
  seq: 100

http:
  method: GET
  url: "{{baseURL}}/auth/me"
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: req.setHeader("Authorization", "");
    - type: tests
      code: |-
        test("Legacy me without auth rejected", function () {
          expect([401, 403]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
