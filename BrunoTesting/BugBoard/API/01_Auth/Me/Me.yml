info:
  name: Me_legacy_auth
  type: http
  seq: 98

http:
  method: GET
  url: "{{baseURL}}/auth/me"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Legacy me status", function () {
          expect([200, 401, 403]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
