info:
  name: Refresh_legacy_valid
  type: http
  seq: 98

http:
  method: POST
  url: "{{baseURL}}/auth/refresh"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Legacy refresh returns expected status", function () {
          expect([200, 401]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
