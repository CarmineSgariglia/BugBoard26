info:
  name: Password_reset_invalid_otp
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/auth/password/reset"
  body:
    type: multipart-form
    data:
      - name: email
        type: text
        value: "{{email}}"
      - name: code
        type: text
        value: "000000"
      - name: newPassword
        type: text
        value: NewStrongPass123!
  auth:
    type: bearer
    token: ""

runtime:
  scripts:
    - type: tests
      code: |-
        test("Password reset invalid otp rejected", function () {
          expect([400, 429]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
