info:
  name: Auth_bootstrap
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/auth/csrf"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("CSRF endpoint reachable", function () {
          expect([200, 204]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
