info:
  name: Create_invalid_blank_name
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/tags"
  body:
    type: multipart-form
    data:
      - name: name
        type: text
        value: "   "
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Blank tag name rejected or forbidden", function () {
          const email = bru.getEnvVar("email") || "";
          if (email.includes("admin")) {
            expect(res.getStatus()).to.equal(400);
          } else {
            expect([403, 404]).to.include(res.getStatus());
          }
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
