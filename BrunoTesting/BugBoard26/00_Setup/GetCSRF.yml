info:
  name: GetCSRF
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/auth/csrf"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("csrf endpoint returns success", function () {
          expect([200, 204]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
