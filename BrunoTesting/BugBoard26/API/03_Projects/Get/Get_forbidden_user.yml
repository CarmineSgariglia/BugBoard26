info:
  name: Get_forbidden_user
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/projects/{{project_id}}
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("Correct project returned", function () {
          const body = res.getBody();
          expect(body.projectId).to.equal(bru.getVar("project_id"));
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
