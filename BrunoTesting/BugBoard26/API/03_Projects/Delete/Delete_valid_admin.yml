info:
  name: Delete_valid_admin
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/projects/{{project_id}}
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 204", function () {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
