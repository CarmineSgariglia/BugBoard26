info:
  name: Update_invalid_body
  type: http
  seq: 2

http:
  method: PATCH
  url: http://localhost:8000/api/projects/{{project_id}}
  body:
    type: multipart-form
    data:
      - name: name
        type: text
        value: Updated Project
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("Project updated", function () {
          const body = res.getBody();
          expect(body.name).to.equal("Updated Project");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
